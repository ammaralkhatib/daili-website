Desktop renders of `/` and `/de/` with both store badges live. The 360px
captures are deliberately absent: headless Chrome does not apply mobile-viewport
emulation, so they came out in desktop layout and would have been misleading.
See the caveat section of ../001-appstore-live.md.
